# Gaze-based desktop UI - minimal dependencies
opencv-python>=4.8.0
# Pin 0.10.8: MediaPipe 0.10.29+ removed mp.solutions (Face Mesh is legacy)
mediapipe==0.10.8
pyautogui>=0.9.54
numpy>=1.24.0

# Optional: ML-based gaze (set USE_EYEGESTURES=True in config). Skip these if you only use the built-in pipeline.
# pip install eyeGestures scikit-learn
eyeGestures>=1.3.4
scikit-learn>=1.2.0
